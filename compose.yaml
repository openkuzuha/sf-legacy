services:
    app:
        build:
            context: .
        ports:
            - '8000:8000'
        environment:
            APP_ENV: prod
            APP_DEBUG: '0'
        volumes:
            - .:/app
            - vendor:/app/vendor

volumes:
    vendor:
