parameters:
    app.title: '%env(APP_TITLE)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'

    App\Post\PostRepository:
        factory: ['@App\Post\PostRepositoryFactory', 'create']
