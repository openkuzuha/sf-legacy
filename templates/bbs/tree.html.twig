{% extends 'base.html.twig' %}

{% import 'bbs/_tree.html.twig' as tree %}

{% block content %}
            <nav><a href="{{ path('app_hello') }}">掲示板へ戻る</a></nav>
            <hr>
            <p class="tree-updated">
                <a href="{{ path('app_thread', {threadId: thread_id}) }}" aria-label="スレッド表示" title="スレッド表示">◆</a>
                更新日：<time datetime="{{ updated_at }}">{{ updated_at|bbs_datetime }}</time>
            </p>
            {{ tree.branches(children, 'root', path('app_thread_tree', {threadId: thread_id})) }}
{% endblock %}
